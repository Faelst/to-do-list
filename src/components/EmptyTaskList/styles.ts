import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  emptyTasksList: {
    alignItems: 'center',
    borderTopColor: '#808080',
    borderTopWidth: 1,
    paddingTop: 60,
    marginTop: 5,
  },
});
